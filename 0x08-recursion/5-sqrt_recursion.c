#include "main.h"
/**
 * _sqrt_recursion - returns square root
 * find_sqrt - finds the natural root
 * @root: dk
 * @num:
 * _sqrt_recursion - returns square root of a number
 * Return: sqrt
 */
int find_sqrt(int num, int root);
int _sqrt_recursion(int n)
{
	int num;
	int root;

	if ((root * root) == num)
		return (root);

	return (0);
}
