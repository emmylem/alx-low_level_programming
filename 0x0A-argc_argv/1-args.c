#include <stdio.h>
#include "main.h"
/**
 * main - prints the number
 * @argc: the parameter
 * @argv: array of command
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
